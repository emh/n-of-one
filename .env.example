# Deployed Worker URL. Local development proxies /api automatically.
VITE_WORKER_URL=https://n-of-one-api.your-subdomain.workers.dev
# Only needed for a known GitHub Pages subpath. Relative assets are the default.
# BASE_PATH=/n-of-one/
